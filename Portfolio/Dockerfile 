# Use official Nginx image (small & fast)
FROM nginx:alpine

# Copy all your website files (HTML, CSS, JS, images, etc.)
COPY . /usr/share/nginx/html

# Expose port 80 to access website
EXPOSE 80

# Run nginx in foreground
CMD ["nginx", "-g", "daemon off;"]